apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: services
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 9898
      targetPort: 80
  type: LoadBalancer